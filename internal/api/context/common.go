package context

const (
	// ContextKeyUserID is the key used to store the user ID in the context
	ContextKeyUserID = "user_id"
	// ContextKeyUserAddress is the key used to store the user's address in the context
	ContextKeyUserAddress = "user_address"
	// ContextKeyAuth is the key used to store the authentication information in the context
	ContextKeyAuth = "auth"
)
